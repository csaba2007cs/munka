# Audio assets

Place `.mp3`, `.wav`, `.ogg`, or `.m4a` files here for `api/audio.php` clip triggers and display background audio.

```
cheer_crowd.mp3  — Éljenző tömeg hang (ElevenLabs fallback)
                   Forrás: megrendelőtől kapott fájl
```
